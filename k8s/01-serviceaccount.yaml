apiVersion: v1
kind: ServiceAccount
metadata:
  name: memory-drive
  namespace: memory-drive
  labels:
    app.kubernetes.io/name: memory-drive
# A dedicated ServiceAccount lets you attach IRSA/IAM roles later and scope
# RBAC precisely instead of using the namespace "default" account.
automountServiceAccountToken: false
